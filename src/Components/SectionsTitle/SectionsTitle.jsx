

const SectionsTitle = ({ heading, subheading }) => {
    return (
        <div>
            <p>{subheading}</p>
            <h3>{heading}</h3>
        </div>
    );
};

export default SectionsTitle;